# -*- coding: utf-8 -*-
"""第二轮压缩：把篇幅继续向编辑部要求的 6000 字符靠拢。"""
import xml.etree.ElementTree as ET
from lib import W, register, rewrite, text_of, find, save

SRC = 'work/word/document.xml'
register(SRC)
tree = ET.parse(SRC)
body = tree.getroot().find(W + 'body')

R = [
    # ── 英文篇首 ──────────────────────────────────────────────────────
    ('Design and Implementation',
     'Design and Implementation of a CDC Research Talent Evaluation System '
     'Based on Dynamic Points and Tag Profiling'),

    ('Abstract:',
     '{b|Abstract: }To improve the granularity of research talent evaluation in '
     'centers for disease control and prevention (CDC), a research points '
     'management system based on Spring Boot and MyBatis is designed. A piecewise '
     'model allocates points among multiple authors. A dynamic tag weight model '
     'using a first-order exponentially weighted moving average embeds an '
     'exponential time decay ahead of behaviour-score normalization, so that tag '
     'weights, capability scores and the talent profile form one dimensionally '
     'consistent chain. A four-level approval workflow engine is built as a finite '
     'state machine without third-party dependency. Over three months at Nantong '
     'CDC, the average approval cycle fell from 7 days to 2 days and the '
     'points-accounting error rate from 3.54% to 0.08%.'),

    ('Key words:',
     '{b|Key words: }points-based evaluation; tag weight model; time decay; '
     'workflow engine; front-end and back-end separation'),

    # ── 引言 ─────────────────────────────────────────────────────────
    ('科教兴卫',
     '在“科教兴卫、人才强卫”战略指引下，各级疾控机构日益重视科研能力建设，但基层疾控'
     '机构的科研人才评价仍存在方式粗放、“四唯”倾向明显、缺少动态量化手段等问题[1-2]，'
     '难以支撑科研梯队建设与精准激励。'),

    ('李涛等[1]',
     '李涛等[1]、沈杨阳[2]与 Amaral 等[3]分别从指标体系、组织机制与经费分配角度探讨了'
     '科研评价，但评价对象均为机构而非个人；Lei[4]面向“破五唯”的分类绩效指标体系与'
     '本文思路接近，但其指标构成与疾控机构的产出结构差异较大；罗仕鉴等[7]与白菁昊等[8]'
     '验证了用户画像与动态标签方法的有效性，但均未考虑科研成果的长周期性与时效衰减。'
     '为此，本文提出面向多作者成果的分段权重积分分配模型，构建“指数加权移动平均标签'
     '权重 + 指数时间衰减”的动态画像模型，并实现无第三方依赖的状态机工作流与可配置'
     '积分规则引擎。'),

    # ── 2 系统总体架构 ────────────────────────────────────────────────
    ('系统采用四层架构',
     '系统采用四层架构：表现层由 Spring Boot 的 @RestController 接收 HTTP 请求并调用'
     '业务服务，实现前后端分离[5]；业务逻辑层封装积分计算、审核流程控制、画像生成与'
     '排行统计等规则并管理事务；数据访问层通过 MyBatis 的 Mapper 接口与 XML 映射文件'
     '实现对象关系映射；数据层负责持久化与存储管理。前端采用 Vue.js，数据库选用 '
     'MySQL 8.0，并引入 Redis 缓存提升查询效率[5-6]。分层架构如图 1 所示。'),

    # ── 3 核心功能模块设计 ────────────────────────────────────────────
    ('系统设计了五大功能模块',
     '系统设计了五大功能模块，构成申报、计算、画像、展示、权限的完整闭环。积分管理模块'
     '支持可视化配置 12 大类 46 项积分规则，成果提交后由计算引擎自动校验、核算分值并'
     '生成积分流水；人才画像模块构建涵盖结构化标签（学历、职称、专业）与能力标签'
     '（科研能力、项目管理、数据分析）的动态标签体系[7-8]；统计分析模块的“人才晴雨表”'
     '仪表盘集成 Apache ECharts，支持多维交叉统计与下钻；申报审核模块内置轻量级工作流'
     '引擎驱动四级审批；系统管理模块采用 RBAC 模型构建用户—角色—权限三层架构[9]，并'
     '承担基础数据维护与操作日志审计。'),

    # ── 4.1 ─────────────────────────────────────────────────────────
    ('传统实现中调整积分规则',
     '传统实现中调整积分规则需修改代码并重新发布。为此，系统将全部积分规则存入数据库'
     '配置表：points_category 存储 12 个成果大类，points_item 定义分值标准、年度上限、'
     '有效期与适用职称范围，points_record 记录积分变动明细，管理员在后台修改配置即可'
     '实时生效。成果校验、重复申报拦截、年度上限判断、按作者序位分配与流水生成由 '
     'Spring 声明式事务封装，任一步骤出错即回滚。'),

    # ── 4.2 ─────────────────────────────────────────────────────────
    ('式中：{i|S}',
     '式中：{i|S}{sub|i} 为第 {i|i} 作者获得的积分；{i|S}{sub|total} 为成果标准积分；'
     '{i|n} 为作者总数。第 1、2、3 作者的分配系数分别为 1.0、0.8、0.5，第 4 作者及以后'
     '按 0.2 计；通讯作者按第 1 作者计，共同第一作者按并列第 1 作者计。'),

    ('名义分值制',
     '式(1)采用{b|名义分值制}而非总量守恒制，即各作者所得之和可能大于成果标准积分'
     '（4 位作者时为 2.5 倍），以契合疾控机构鼓励多科室协作攻关的导向；其“挂名”风险由 '
     'points_item 表的年度上限封顶与第 4 作者及以后积分不计入职称评审口径两项制度约束'
     '控制，与常见守恒型方法的对比如表 1 所示。对需控制积分总量的场景，系统另提供'
     '归一化模式（式(3)）。'),

    ('表 1  多作者', '表 1  多作者积分分配方法对比'),

    # ── 4.3 ─────────────────────────────────────────────────────────
    ('式(5)中 {i|α}',
     '式(5)中 {i|α} 取 0.7，对应记忆长度 1/(1 − {i|α}) ≈ 3.3 个周期，即最新一年贡献'
     '约占 70%、历史累积约占 30%；时间衰减系数取 {i|λ} = 0.14，对应半衰期 ln2{i|/λ} '
     '≈ 4.95 年、年衰减约 13.1%，既避免成果永久有效导致评价僵化，也防止衰减过快挫伤'
     '长期积累型科研人员的积极性。'),

    ('标签更新采用双策略',
     '标签权重在积分流水生成后 1 s 内由 Spring Event 触发准实时微调，并于每日 2:00 '
     '全量重算活跃用户近一年的权重。'),

    # ── 4.4 ─────────────────────────────────────────────────────────
    ('定义标签—能力维度映射矩阵',
     '定义标签—能力维度映射矩阵 {i|M} ∈ R{sup|T×D}，{i|T} 为标签总数，{i|D} 为能力'
     '维度数（本系统取 {i|D} = 16，各维度名称如图 2 所示）。元素 {i|m}{sub|t,d} ∈ '
     '[0, 1] 表示标签 {i|t} 对维度 {i|d} 的贡献度，满足 Σ{sub|d}{i|m}{sub|t,d} = 1，'
     '由 3 名科研管理专家按德尔菲法两轮打分确定。各维度原始得分为标签权重的加权和'
     '（式(6)）；由于式(5)已完成时间衰减修正，式(6)无需再次引入衰减因子。'),

    ('为消除维度间的量纲差异', '为消除量纲差异，对原始得分作归一化处理（式(7)）。'),

    ('式中：{i|C}',
     '式中：{i|C}{sub|d,min} 与 {i|C}{sub|d,max} 分别为全中心用户在维度 {i|d} 上的最低'
     '与最高原始得分。映射至 [20, 100] 而非 [0, 100]，可避免最低分用户恒为 0、雷达图'
     '退化为尖刺；当 {i|C}{sub|d,max} = {i|C}{sub|d,min} 时取 {i|C}′{sub|d,u} = 60。'
     '式(7)适用于同一周期内的横向比较，跨年度比较以上线首年分布为固定基准；归一化得分'
     '直接对应雷达图半径。'),

    # ── 4.5 ─────────────────────────────────────────────────────────
    ('针对疾控中心审核流程',
     '针对疾控中心审核流程相对固定但存在微调需求的特点，本文实现了无第三方框架依赖的'
     '状态机工作流引擎。引擎定义草稿、待初审、初审驳回、待复核、复核驳回、待终审与'
     '终审通过 7 种状态及 7 种转换事件：草稿经提交进入待初审，初审、复核通过后依次进入'
     '待复核、待终审，任一环节驳回则回退至对应驳回态。各状态的处理人、处理时限与'
     '下一状态由 workflow_config 表配置；事件触发时引擎依次校验权限、更新状态、记录'
     '日志并通知下一处理人，终审通过时触发积分计算。管理员修改配置即可调整审核环节，'
     '无需重启系统。'),

    # ── 4.6 ─────────────────────────────────────────────────────────
    ('系统引入 Redis 缓存',
     '系统引入 Redis 缓存应对高频积分查询，采用 Cache-Aside 模式并以空值缓存与'
     '分布式锁分别防止缓存穿透与击穿，实测缓存命中率在 50～500 并发下保持在 92.5% '
     '以上（表 3）；数据库层面针对核心查询路径建立联合索引并优化 InnoDB 配置。安全'
     '方面整合 Spring Security 与 JWT 实现无状态认证[10]；涉及的学历、职称与成果数据'
     '按《中华人民共和国个人信息保护法》实施最小必要采集与分级授权访问，敏感字段脱敏'
     '并留存审计日志。'),

    # ── 5 ───────────────────────────────────────────────────────────
    ('测试环境为',
     '测试环境为 4 核 CPU / 32 GB 内存、Ubuntu 24.04 LTS、OpenJDK 21、MySQL 8.0 与 '
     'Redis 7.2，压力测试工具为 Apache JMeter。按五大模块设计的功能测试结果如表 2 '
     '所示，核心接口性能与缓存命中率如表 3 所示。'),

    ('为评价标签准确性',
     '为评价标签准确性，从 270 名科研人员中随机抽取 90 名，由 3 名专家依据其近三年'
     '科研档案独立标注，以三人一致的结果为金标准与系统输出比对。系统共生成 32 个动态'
     '能力标签，准确率 88.0%、召回率 84.1%、{i|F}1 值 0.86，专家标注 Kappa 系数 0.81。'
     '上线前后各 3 个月的应用效果对比如表 4 所示。'),

    ('由表 4 可见',
     '由表 4 可见，系统上线后申报与审核全流程线上化，申报单量增至上线前的 7.4 倍，'
     '平均审核周期由 7 d 降至 2 d，科研人员参与度明显提升。'),

    # ── 6 ───────────────────────────────────────────────────────────
    ('本文设计并实现了基于',
     '本文设计并实现了基于 Spring Boot 与 MyBatis 的疾控科研积分管理系统：针对多作者'
     '成果提出分段权重分配模型并与守恒型方法作了对比；将指数时间衰减前置于行为积分'
     '归一化环节，使标签权重、能力维度得分与人才画像形成量纲一致的计算链路；基于'
     '有限状态机实现了无第三方依赖的审批工作流引擎。后续将开展 {i|α}、{i|λ} 的敏感性'
     '分析，并与人事、财务等系统实现数据互通。'),
]

# 标记语言里的花括号只在替换文本中出现，定位串需去掉标记
import re
from lib import TOK
for needle, new in R:
    key = TOK.sub(lambda m: m.group(2), needle)
    rewrite(find(body, key), new)

save(tree, SRC, '../v8/unz/word/document.xml')
print('第二轮压缩完成，共重排 %d 段。' % len(R))
