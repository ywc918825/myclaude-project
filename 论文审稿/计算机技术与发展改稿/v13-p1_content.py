# -*- coding: utf-8 -*-
"""P1 把 2026-09-02 版 .doc 的内容改动移植进已完成期刊适配的 v12 稿。

作者在新版中已自行修正的三处予以保留：邮箱对调、引言去人名化、表4 表述收敛。
与本轮 s8 冲突的一处（τ_now 定义）以 s8 为准——作者已口头确认代码取当前时刻，
新版 .doc 正文仍写"本周期期末……不重复计算同一段时间"，属未同步的旧表述。
"""
import sys, copy, xml.etree.ElementTree as ET
sys.path.insert(0, '.')
import lib
from lib import W, rewrite, text_of, find, save

SRC = 'unz/word/document.xml'
lib.register(SRC)
tree = ET.parse(SRC)
body = tree.getroot().find(W + 'body')
n = 0


def sub(anchor, old, new):
    """在含 anchor 的段落里做一次精确替换。"""
    global n
    p = find(body, anchor)
    t = text_of(p)
    assert old in t, '未命中【%s】:\n%s' % (old[:40], t[:260])
    rewrite(p, t.replace(old, new))
    n += 1


# ── 1. 摘要：删除“15 个业务科室” ───────────────────────────────────
sub('摘　要：', '覆盖 15 个业务科室、270 名科研人员', '覆盖 270 名科研人员')
sub('Abstract: Research talent evaluation',
    'covering 15 departments and 270 researchers', 'covering 270 researchers')

# ── 2. 引言第二段：改为去人名化综述 ────────────────────────────────
p = find(body, '李涛等[1]从指标体系角度探讨了科研评价')
rewrite(p,
    '现有研究多从指标体系视角探讨科研评价[1]，亦有工作面向疾控信息化评价指标'
    '展开[3]，但其评价对象聚焦机构层面而非个人；围绕“破五唯”导向下的科研成果评价，'
    '相关研究分别从绩效拨款、组织机制与分类绩效等角度切入[4-7]，评价对象同样以'
    '机构为主；在用户画像与时序动态标签方面，已有方法验证了其有效性[8-9]，'
    '并将指数加权移动平均与时间衰减引入用户兴趣建模[10-11]，'
    '但均未考虑科研成果的长周期性与时效衰减。两类研究尚缺乏贯通：'
    '评价指标研究停留在机构层面，动态标签研究面向的是消费与学习行为。'
    '本文直接将成果积分作为行为信号接入标签权重更新，再经标签权重映射到能力维度，'
    '使量化与画像共用一条计算通路。沿此通路，多作者成果的积分如何分配、'
    '标签权重如何随时间衰减、审批环节如何免发布调整，是须先解决的三个具体问题。')
n += 1

# ── 3. 1 节 总体架构：引文重编号 + 改写技术栈佐证句 ────────────────
sub('系统采用四层架构',
    '该技术栈在科研样本库、考试等业务系统中已有成熟应用[12,17]。'
    '可视化层集成 Apache ECharts[16]',
    '已有文献[12,16]分别以 Spring Boot 与 Flask 实现了科研样本库与在线考试系统，'
    '表明轻量级 Web 技术栈在此类业务中的适用性。'
    '可视化层集成 Apache ECharts[17]')

# ── 4. 2 节 功能模块：16 个维度表述 + ECharts 编号 ─────────────────
sub('系统设计了五大功能模块',
    '专业方向与能力维度（科研能力、项目管理、数据分析）构成的三维动态标签体系[8-9]',
    '专业方向与能力维度（科研能力、项目管理、数据分析等 16 个维度）'
    '构成的动态标签体系[8-9]')
sub('系统设计了五大功能模块',
    '仪表盘集成 Apache ECharts[16]', '仪表盘集成 Apache ECharts[17]')

# ── 5. 3.2 节：引文重编号 ──────────────────────────────────────────
sub('多作者成果的积分分配是量化评价的难点',
    '会抹杀普通合著者的贡献[23]，而分数分配法、调和计数法等经典方案[24]',
    '会抹杀普通合著者的贡献[22]，而分数分配法、调和计数法等既有方案[23]')

# ── 6. 式(1) 式中说明：补通讯作者/共同一作特例 ──────────────────────
sub('Si 为第 i 作者获得的积分',
    '通讯作者按第 1 作者计，共同第一作者按并列第 1 作者计。',
    '通讯作者按第 1 作者计，共同第一作者按并列第 1 作者计。'
    '当成果存在通讯作者（非第 1 作者）或共同第一作者时，'
    '相应作者均按第 1 作者系数 1.0 计，合计上限相应放宽。')

# ── 7. 名义分值段：引文重编号 + 补归一化重算说明 ────────────────────
sub('式(1)采用名义分值制而非总量守恒制',
    '取自文献[23,24]的归类', '取自文献[22,23]的归类')
sub('式(1)采用名义分值制而非总量守恒制',
    '需控制年度积分总量的科室可在后台切换为归一化模式（式(3)）。',
    '需控制年度积分总量的科室可在后台切换为归一化模式（式(3)）。'
    '名义分值主要用于激励与画像展示；在需要横向比较或用于职称评审排名时，'
    '系统按归一化模式重算，以消除合作人数差异对单篇成果积分总量的放大效应。')

# ── 8. 式(4)(5) 式中说明：S_max 定义微调（保留 s8 的 τ_now 修正）───
sub('为用户 u 在标签 t 上第 k 个周期',
    'Smax,t 为本周期全中心在标签 t 下的最高行为积分',
    'Smax,t 为该行为所属周期全中心在标签 t 下的最高行为积分')

# ── 9. 3.3 参数段：补 j 的定义（并入 s8 改写后的版本）──────────────
sub('式(5)中 α 取 0.7',
    '各期权重按 α(1 − α)j 衰减；',
    '各期权重按 α(1 − α)j 衰减（j 为自当前周期往回数的期序，'
    '区别于式(5) 的求和下标 i）；')

# ── 10. 3.5 工作流引擎：补与已有轻量级方案的对比 ────────────────────
sub('针对疾控中心审核流程相对固定',
    '相比基于 BPMN 2.0 的全流程引擎或依赖 Drools 等规则引擎的方案[25-26]，'
    '本引擎仅保留流程定义、实例管理与状态流转三类职责，以状态机为核心驱动。',
    '已有轻量级工作流引擎方案[24-25]多聚焦流程定义与实例流转；'
    '本文进一步引入状态机驱动与配置化审批，相比基于 BPMN 2.0 的全流程引擎或'
    '依赖 Drools 等规则引擎的重型方案，本引擎仅保留流程定义、实例管理与'
    '状态流转三类职责。')

# ── 11. 3.6 性能与安全：引文重编号 + 弱化强绑定表述 ─────────────────
sub('系统引入 Redis 缓存应对高频积分查询',
    '热点键的识别与分区沿用集群缓存中的热点合并做法[27]',
    '热点键的识别与热度合并参考了缓存热点研究的通用做法[26]')
sub('系统引入 Redis 缓存应对高频积分查询',
    '数据库层面按持久内存 B+ 树索引的优化结论[28]为核心查询路径建立联合索引',
    '数据库层面借鉴索引列顺序与选择性的一般性优化结论[27]'
    '为核心查询路径建立联合索引')
sub('系统引入 Redis 缓存应对高频积分查询',
    '整合 Spring Security 与 JWT 实现无状态认证[22,29]',
    '整合 Spring Security 与 JWT 实现无状态认证[28,29]')

# ── 12. 4 节 测试环境：措辞收敛 ────────────────────────────────────
sub('测试环境为 4 核 CPU',
    '并发梯度按开源 Web 项目性能测试的常见做法设置[30]',
    '并发梯度参考开源 Web 项目性能测试的实践设置[30]')

# ── 13. 标签准确率：补不一致样本处理与 Kappa 口径 ───────────────────
sub('为评价标签准确性',
    '以三人一致的结果为金标准与系统输出比对。',
    '以三人一致的结果为金标准与系统输出比对；'
    '专家意见不一致的样本（约 3–5 例）经协商讨论确定最终标注，并全部纳入统计。')
sub('为评价标签准确性', '专家标注 Kappa 系数 0.81',
    '专家标注两两平均 Cohen’s Kappa 系数 0.81')

# ── 14. 表4 讨论：采用作者收敛后的表述（保留本轮补的口径说明）───────
sub('由表4可见', '科研人员参与度明显提升。',
    '科研人员线上申报的参与程度显著提升。')

# ── 15. 结论：机构全称 + 补式(4) 极值归一化局限 ─────────────────────
sub('本文工作尚存三点局限', '系统在南通市疾病预防控制中心运行 3 个月',
    '系统在南通市疾病预防控制中心运行 3 个月')   # 已是全称，占位校验
sub('本文工作尚存三点局限', '本文工作尚存三点局限：', '本文工作尚存四点局限：')
sub('本文工作尚存三点局限' if False else '本文工作尚存四点局限',
    '其三，标签—能力维度映射矩阵由本中心专家标定，跨机构推广时需重新标定。',
    '其三，标签—能力维度映射矩阵由本中心专家标定，跨机构推广时需重新标定；'
    '其四，式(4) 采用极值归一化，对周期内出现的异常高分较为敏感。')

# ── 16. 邮箱：采用作者修正后的对应关系 ─────────────────────────────
sub('作者简介：杨文超', 'E-mail: 573816315@qq.com', 'E-mail: yangwecy@gmail.com')
sub('张卫兵（1971—）', 'E-mail: yangwecy@gmail.com', 'E-mail: 573816315@qq.com')
sub('收件人：', 'E-mail: 573816315@qq.com', 'E-mail: yangwecy@gmail.com')

save(tree, SRC, 'ref.xml')
print('P1 done: %d 处内容已移植' % n)
